* list comprehension
>>> S = [x**2 for x in range(10)]
>>> V = [2**i for i in range(13)]
>>> M = [x for x in S if x % 2 == 0]

* python-way
* переписать цикл
переписал цикл

* for i in l: i = int(i)  # рассказать почему не работает
не работает потому, что это ссылка и дальше мы эту ссылку хотим поменять мы не можем поменять знаечние этой ссылки на какое-то число это будет уе совсем другая переменная
потому надо создавать новый список
нельзя обращаться так к списку

* исправить все предупреждения IDE
done

* что такое генератор
генерирует каждое значение при вызове, а не держит в памяти и  можно пробежать только один раз

* переписать без words.count()  # рассказать почему неправильно
переписал
потому что мы будем постоянно бегать по массиву (столько сколько всего слов)

* как на английском будет медиана
median

* что такое set, чем отличается от списка
кроме того что не содержит одинаковых элементов можно использовать операции такие как пересечение объединение и тд